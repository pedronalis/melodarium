// src/EmptyPane.qml — stub, substituído pela fatia podcast-vazio
import QtQuick
import Melodia.App

Rectangle {
    color: Theme.mSurfaceVariant
    radius: Theme.radiusM
    border.width: Theme.borderS
    border.color: Theme.mOutline
}
